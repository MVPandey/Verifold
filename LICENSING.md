# License scope

The MIT license in `LICENSE` applies to the Verifold harness and its supporting files:

- `src/cli.ts` and `src/cli/`.
- The shared CLI dependencies `src/domain/profile.ts` and `src/ui/dom.ts`.
- The compiled equivalents included in the `verifold` npm package.
- `scripts/`, `tests/`, `.githooks/`, and `.github/`.
- Root package, build, lint, formatting, and validation configuration files.
- `README.md`, `SECURITY.md`, this file, and documentation about the CLI in `docs/`.

The website implementation and visual brand assets are not covered by this MIT grant. This includes `src/main.ts`, `src/style.css`, `src/ui/views.ts`, `index.html`, and `public/assets/`. Other repository files outside the list above remain reserved unless they contain a separate license notice.

The Manrope font in `public/fonts/` retains its SIL Open Font License. Third-party components retain their own licenses.

The MIT grant does not grant rights to Verifold trademarks. The published npm package excludes the website and raster brand assets.
