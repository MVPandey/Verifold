Use the existing host session and selected idea to prepare a task-specific Automative pilot plan. Verifold has printed a planning request, not started an execution adapter. Selection of an idea does not authorize an experiment.

Define the scientific question, competing hypotheses, and the smallest experiment that can change the next decision. Explain why its outcome would be informative, including a negative or inconclusive result. Ground the design in the reviewed evidence and identify assumptions that still need checking.

Specify the baseline, intervention, controls, datasets or generated inputs, model and dependency versions, sample or seed policy, and measured outcomes as appropriate. Describe the evaluator separately from the system being optimized. Identify protected evaluator files, held-out evidence, leakage risks, and how evaluator changes would receive review. Do not invent approved thresholds or hide an unresolved metric definition.

Propose scope, resource budget, time limits, allowed operations, cancellation, recovery, and verification gates appropriate to this pilot. Explain which values come from the user, which are estimates, and which require a decision. Define when to continue, stop, or label a result inconclusive. Preserve failed attempts, configurations, logs, and result evidence needed to explain or reproduce the outcome.

Inspect available Automative instructions and capabilities before claiming that a command or adapter can execute the plan. If integration is unavailable, return a useful handoff with the missing setup identified. Do not present a plan, simulated output, or model report as an executed experiment.

Present the concrete goal, protected evaluator, budget, and gates for user approval before execution. Do not ask again for unchanged scope already approved in the current task. Do not edit host permissions or install hooks automatically. Leave the researcher with a reviewable plan and a clear next action, not an unsupported promise of automation.
