#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
if ! git diff --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
  echo 'Stage the complete intended snapshot before committing; validation must match the index.' >&2
  exit 1
fi
exec make validate
