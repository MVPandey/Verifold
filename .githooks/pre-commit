#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
if ! git diff --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
  echo 'Unstaged or untracked files exist. Stage the intended changes or preserve unrelated files before committing.' >&2
  exit 1
fi
exec make validate
